# Local Progress

## MVP

## 阶段 1：项目初始化（已完成）

日期：2026-05-16

### 目标
创建可本地启动的最小项目骨架（不实现业务功能）。

### 本阶段完成
- 建立 monorepo 基础结构：`backend/`、`frontend/`、`docs/`、`docker-compose.yml`、`README.md`。
- Backend 使用 Go + Echo 搭建最小服务。
- 新增 `GET /health` 健康检查接口。
- 支持 `go run ./cmd/api` 启动后端。
- 按设计书创建后端基础分层目录：`handler/usecase/repository/domain/service/middleware/config`。
- 创建 `backend/migrations/` 目录（仅占位，未建完整表结构）。
- Frontend 使用 React + TypeScript + Vite + MUI 搭建最小页面。
- 页面仅展示 `Novel Agent` 和简要说明。
- `docker-compose.yml` 增加 MySQL 服务。
- README 补充 backend/frontend/mysql 本地启动步骤。

### 刻意未做（符合 MVP 阶段限制）
- Auth（注册/登录）
- Novel / Character / Chapter CRUD
- OpenAI 调用
- RAG / MCP / Google Drive / 多 Agent

### Git 过程记录
- 分支创建：`feat/mvp-phase1-init`
- 功能提交：`b1614de chore: initialize phase 1 project skeleton`
- 合并到 `main`：`4cd0fe9 Merge branch 'feat/mvp-phase1-init'`
- 已推送远端：`origin/main`

### 复习要点
- 先保证“可启动 + 结构正确”，再进入下一阶段功能开发。
- `frontend/node_modules` 已通过 `.gitignore` 排除，避免误提交依赖目录。

## 阶段 2：后端基础设施打底（已完成）

日期：2026-05-16

### 目标
在不实现业务功能的前提下，补齐后端配置读取、数据库连接骨架、路由接线与 migration 占位。

### 本阶段完成
- 新增配置模块：`backend/internal/config/config.go`。
- 支持环境变量读取：`PORT`、`MYSQL_DSN`、`JWT_SECRET`（JWT 仅占位，未进入业务逻辑）。
- 新增数据库连接骨架：`backend/internal/repository/db.go`。
- 启动时完成 MySQL 连接初始化与 `Ping` 检查，失败即退出。
- 重构 `backend/cmd/api/main.go` 为 `config -> db -> router` 初始化流程。
- 保留 `GET /health`，新增 `GET /health/db`。
- 新增 migration 占位文件：
  - `backend/migrations/202605160001_init_placeholder.up.sql`
  - `backend/migrations/202605160001_init_placeholder.down.sql`
- README 更新了第 2 阶段启动与接口说明。

### 刻意未做（符合本阶段限制）
- 登录/注册
- Novel / Character / Chapter CRUD
- OpenAI 调用与章节生成
- 任何 RAG / MCP / Google Drive / 多 Agent 功能
- 完整业务表 migration

### 运行与验证
- 通过：`go test ./...`（backend 包可编译）。
- 未完成：`docker compose up -d mysql` 在当前环境无法连接 Docker daemon，故未完成本地容器联调。

### 复习要点
- 阶段 2 重点是“运行链路打底”而不是业务实现。
- 启动即检查 DB 连通性，有助于尽早暴露环境配置问题。

## 阶段 3：用户认证最小闭环（已完成）

日期：2026-05-17

### 目标
实现 MVP 认证最小闭环：注册、登录、获取当前用户信息（JWT）。

### 本阶段完成
- 新增 `users` migration：
  - `backend/migrations/202605170001_create_users_table.up.sql`
  - `backend/migrations/202605170001_create_users_table.down.sql`
- `users` 表包含：`id/name/email/password_hash/created_at/updated_at`，并对 `email` 添加唯一约束。
- 新增用户领域模型：`backend/internal/domain/user.go`。
- 新增用户仓储：`backend/internal/repository/user_repository.go`，支持创建用户、按 ID 查询、按邮箱查询。
- 新增认证用例：`backend/internal/usecase/auth_usecase.go`。
  - 注册时使用 `bcrypt` hash 密码。
  - 登录时校验密码。
  - 使用 JWT 生成 token。
- 新增 JWT 中间件：`backend/internal/middleware/auth.go`。
- 新增认证 Handler：`backend/internal/handler/auth_handler.go`。
- 路由接入：
  - `POST /auth/register`
  - `POST /auth/login`
  - `GET /auth/me`（需 Bearer Token）

### 刻意未做（符合本阶段限制）
- 前端登录/注册页面
- Novel / Character / Chapter CRUD
- OpenAI 调用

### 运行与验证
- 通过：`go test ./...`（backend 编译通过）。

### 复习要点
- 密码只存 `password_hash`，不存明文。
- `auth/me` 必须通过 JWT 中间件读取 `user_id`。

## 阶段 4：认证前端最小闭环（已完成）

日期：2026-05-17

### 目标
为已完成的认证后端提供最小前端闭环：注册、登录、获取当前用户。

### 本阶段完成
- 新增前端 API 层：`frontend/src/api/auth.ts`。
- 新增认证页面：`frontend/src/pages/AuthPage.tsx`。
- 新增通用组件：`frontend/src/components/UserProfileCard.tsx`。
- 更新入口：`frontend/src/main.tsx` 改为渲染认证页面。
- 增加本地代理：`frontend/vite.config.ts` 将 `/auth`、`/health` 代理到 `http://localhost:8080`。
- 页面支持：
  - 注册
  - 登录
  - token 持久化（`localStorage`）
  - 调用 `/auth/me` 拉取当前用户
  - 退出登录

### 刻意未做（符合本阶段限制）
- 小说、人物、章节页面与 CRUD
- 复杂 UI 设计
- 全局状态管理库

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 前端采用最小闭环验证接口正确性，避免后端接口一次性做大后返工。
- 本地开发通过 Vite proxy 解决跨域问题。
- 当前 `Current User + Logout` 区块为联调用临时占位 UI，仅用于验证 auth 闭环，后续将由正式业务首页替换。

## 阶段 5：Novel 项目 CRUD（后端最小闭环，已完成）

日期：2026-05-17

### 目标
实现小说项目最小后端闭环（仅 novels），并确保所有操作按 `user_id` 做归属校验。

### 本阶段完成
- 新增 `novels` 表 migration：
  - `backend/migrations/202605170002_create_novels_table.up.sql`
  - `backend/migrations/202605170002_create_novels_table.down.sql`
- 新增领域模型：`backend/internal/domain/novel.go`。
- 新增仓储层：`backend/internal/repository/novel_repository.go`。
  - 创建
  - 当前用户列表
  - 当前用户详情
  - 当前用户更新
  - 当前用户删除
- 新增用例层：`backend/internal/usecase/novel_usecase.go`。
  - `title` 必填
  - `recent_chapter_count` 默认 3
  - 不存在或无权限场景统一返回未找到
- 新增处理层：`backend/internal/handler/novel_handler.go`。
- 路由接入（全量 JWT 保护）：
  - `POST /novels`
  - `GET /novels`
  - `GET /novels/:id`
  - `PUT /novels/:id`
  - `DELETE /novels/:id`

### 刻意未做（符合本阶段限制）
- characters / chapters CRUD
- OpenAI 调用
- novels 前端页面

### 运行与验证
- 通过：`go test ./...`（backend 编译通过）。

### 复习要点
- 所有 novel 查询/更新/删除都必须带 `user_id` 过滤，防止跨用户访问。

## 阶段 6：Novel 前端最小闭环（已完成）

日期：2026-05-17

### 目标
接入 novels 后端接口，完成登录后可用的最小 novels 前端页面。

### 本阶段完成
- 新增 novels API：`frontend/src/api/novels.ts`。
  - 列表：`GET /novels`
  - 新建：`POST /novels`
  - 删除：`DELETE /novels/:id`
- 新增页面：`frontend/src/pages/NovelsPage.tsx`。
  - novel 列表展示
  - 新建表单（title/genre/language）
  - 删除按钮
- 更新认证页：`frontend/src/pages/AuthPage.tsx`。
  - 登录成功后从 Auth 视图切换到 NovelsPage
- 更新开发代理：`frontend/vite.config.ts` 增加 `/novels` 代理。
- 补充依赖：`@mui/icons-material`（与 MUI 7 兼容版本）。

### 刻意未做（符合本阶段限制）
- novels 详情页/编辑页
- characters / chapters 前端页面
- 全量路由体系（react-router）

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 先打通最小业务闭环（登录 -> novels 列表/新增/删除），再扩展功能深度。

## 阶段 7：Novel 编辑功能（前端，已完成）

日期：2026-05-17

### 目标
在 novels 前端列表中增加编辑能力，形成 create/list/update/delete 的可视化闭环。

### 本阶段完成
- 新增前端 API：`PUT /novels/:id`（`updateNovel`）。
- novels 列表项新增编辑按钮。
- 点击编辑后将当前 novel 字段回填到表单。
- 表单状态支持创建/编辑两种模式切换：
  - 创建模式：`Create`
  - 编辑模式：`Update` + `Cancel`
- 保存编辑后更新列表项。

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 先复用同一个表单做 create/update，减少页面复杂度和维护成本。

## 阶段 7.5：Novels 页面结构重构（已完成）

日期：2026-05-17

### 目标
将 novels 页面改为更接近正式产品的信息架构：先看“我的小说”列表，再进入单本小说详情进行编辑。

### 本阶段完成
- 新增页面视图模式：
  - 列表视图（`我的小说`）
  - 新建视图（`新建小说`）
  - 详情编辑视图（单本小说）
- 列表项支持点击进入详情编辑。
- 详情页支持返回列表、更新、删除。
- 复用现有折叠表单组件，减少重复代码。
- 保留右上角头像菜单（用户设置占位 + 退出登录）。

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 先统一信息架构，再逐步扩展功能，可显著降低后续页面重构成本。

## 阶段 8：Characters 后端最小闭环（已完成）

日期：2026-05-17

### 目标
实现 characters 后端 CRUD，并确保通过 `novel_id -> user_id` 链路完成权限隔离。

### 本阶段完成
- 新增 migration：
  - `backend/migrations/202605170003_create_characters_table.up.sql`
  - `backend/migrations/202605170003_create_characters_table.down.sql`
- 新增领域模型：`backend/internal/domain/character.go`。
- 新增仓储层：`backend/internal/repository/character_repository.go`。
  - 通过 `JOIN novels` + `user_id` 校验归属，防止跨用户访问。
- 新增用例层：`backend/internal/usecase/character_usecase.go`。
- 新增处理层：`backend/internal/handler/character_handler.go`。
- 路由接入（JWT 保护）：
  - `POST /novels/:novelId/characters`
  - `GET /novels/:novelId/characters`
  - `GET /novels/:novelId/characters/:id`
  - `PUT /novels/:novelId/characters/:id`
  - `DELETE /novels/:novelId/characters/:id`

### 刻意未做（符合本阶段限制）
- characters 前端页面
- chapters CRUD
- OpenAI 调用

### 运行与验证
- 通过：`go test ./...`（backend 编译通过）。

### 复习要点
- 人物数据权限不能只看 `character.id`，必须沿 `character.novel_id -> novels.user_id` 校验。

## 阶段 9：Characters 前端最小闭环（已完成）

日期：2026-05-17

### 目标
在小说详情页接入人物管理，形成 characters 的前后端闭环。

### 本阶段完成
- 新增 characters API：`frontend/src/api/characters.ts`。
  - `GET /novels/:novelId/characters`
  - `POST /novels/:novelId/characters`
  - `PUT /novels/:novelId/characters/:id`
  - `DELETE /novels/:novelId/characters/:id`
- 新增组件：`frontend/src/components/CharacterManager.tsx`。
  - 人物列表
  - 新建人物
  - 编辑人物
  - 删除人物（含二次确认）
- 在小说详情页挂载人物管理区：`frontend/src/pages/NovelsPage.tsx`。

### 刻意未做（符合本阶段限制）
- 独立人物管理页面
- 高级筛选/搜索
- 章节与 OpenAI 逻辑

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 先把人物管理嵌入小说详情，确保数据关系（novel -> characters）在使用路径上自然成立。

## 阶段 9.5：前端导航结构重构（已完成）

日期：2026-05-17

### 目标
将 novels/characters 相关页面重构为可扩展的左侧导航结构，便于后续功能增加。

### 本阶段完成
- 新增左侧主导航：
  - 我的小说
  - 新建小说
- 在“我的小说”下新增二级导航：
  - 小说详情
  - 小说人物
- 右侧内容区按导航切换：
  - 小说详情：显示 My Novels 列表内容
  - 小说人物：显示 Characters 管理（Character Manager）
  - 新建小说：显示新建小说表单
- 保留右上角用户头像菜单（设置占位 + 退出登录）。

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 先搭建稳定导航骨架，再叠加业务内容，可显著降低后续页面重构成本。

## 阶段 9.6：导航与角色交互修正（已完成）

日期：2026-05-17

### 目标
修正 9.5 重构后的可用性问题，使小说详情/角色管理交互符合实际使用习惯。

### 本阶段完成
- 导航层级与高亮逻辑修正：
  - 父子模块视觉层级调整为“父项清晰、子项缩进”。
  - 点击“新建小说”时，不再错误影响“我的小说”子模块展开状态。
  - 非当前模块不再出现错误高亮。
- 小说详情交互修正：
  - `Novel Detail Editor` 仅在点击具体小说的编辑按钮后显示。
  - 新建小说成功后不显示 `Novel Detail Editor`。
  - 更新小说成功后自动收起 `Novel Detail Editor`。
  - 编辑区补充 `Cancel` 按钮，支持主动退出编辑态。
- 小说人物命名与结构修正：
  - 子模块名称由“小说人物”统一为“小说角色”。
  - “小说角色”页增加小说切换下拉，支持选择当前操作的小说。
  - 在“选择小说”和“新增角色”之间显示当前角色列表。
  - 角色删除保持二次确认弹窗。
- 角色创建/编辑流程重构：
  - “新增角色”改为按钮触发，点击后才显示 `Character Manager`。
  - 新增角色成功后自动回到“小说角色”主视图，并刷新角色列表。
  - 新增角色时，角色列表与新增按钮在编辑器显示期间隐藏。
  - 增加角色编辑入口：点击某角色编辑后进入编辑器。
  - 角色编辑期间隐藏“新增角色”和其他角色列表；保存或取消后回到主视图并刷新。
  - `CharacterManager` 支持 `initialCharacter`，统一处理创建/编辑模式。

### 运行与验证
- 通过：`npm run build`（frontend，多次构建通过）。

### 复习要点
- 导航重构后，最容易出问题的是“状态耦合”（模块展开、当前高亮、编辑态切换），应先定义清晰状态机再落地 UI。
- 创建/编辑类表单建议使用“列表态 vs 编辑态”两态切换，避免同屏过多操作入口导致误操作。

## 阶段 10：Chapters 后端最小闭环（已完成）

日期：2026-05-17

### 目标
实现 chapters 后端 CRUD，并确保通过 `novel_id -> user_id` 链路完成权限隔离。

### 本阶段完成
- 新增 migration：
  - `backend/migrations/202605170004_create_chapters_table.up.sql`
  - `backend/migrations/202605170004_create_chapters_table.down.sql`
- 新增领域模型：`backend/internal/domain/chapter.go`。
- 新增仓储层：`backend/internal/repository/chapter_repository.go`。
  - 通过 `JOIN novels` + `user_id` 校验归属，防止跨用户访问。
  - 同一小说内 `chapter_number` 唯一（由 DB 唯一索引保证）。
- 新增用例层：`backend/internal/usecase/chapter_usecase.go`。
  - 校验 `chapter_number > 0`
  - 校验 `word_count >= 0`
- 新增处理层：`backend/internal/handler/chapter_handler.go`。
- 路由接入（JWT 保护）：
  - `POST /novels/:novelId/chapters`
  - `GET /novels/:novelId/chapters`
  - `GET /novels/:novelId/chapters/:id`
  - `PUT /novels/:novelId/chapters/:id`
  - `DELETE /novels/:novelId/chapters/:id`

### 刻意未做（符合本阶段限制）
- `POST /novels/:novelId/chapters/generate`
- `GET /chapters/:id/export/markdown`
- OpenAI 调用与结构化解析保存

### 运行与验证
- 通过：`go test ./...`（backend 编译通过）。

### 复习要点
- 章节权限同样不能只看 `chapter.id`，必须沿 `chapter.novel_id -> novels.user_id` 校验。
- 先固化章节 CRUD 契约，再做前端页面，可减少联调成本。

## 阶段 11：Chapters 前端最小闭环（已完成）

日期：2026-05-17

### 目标
在现有导航结构中接入章节管理，完成 chapters 前后端最小联通。

### 本阶段完成
- 新增章节 API：`frontend/src/api/chapters.ts`。
  - `GET /novels/:novelId/chapters`
  - `POST /novels/:novelId/chapters`
  - `PUT /novels/:novelId/chapters/:id`
  - `DELETE /novels/:novelId/chapters/:id`
- `NovelsPage` 新增“小说章节”二级导航。
- 新增章节管理区：
  - 小说切换下拉
  - 章节列表展示（章节号/标题/字数）
  - 新建章节
  - 编辑章节
  - 删除章节（含二次确认弹窗）
- 章节编辑采用与既有交互一致的“列表态 / 编辑态”切换：
  - 编辑器显示时隐藏列表与新增按钮
  - 保存或取消后回到列表态并刷新

### 刻意未做（符合本阶段限制）
- AI 生成章节页面与 `generate` 调用
- Markdown 导出按钮与导出流程
- 复杂筛选、分页、富文本编辑

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 先把章节 CRUD 在单页中打通，再拆分独立章节页面可降低重构风险。
- 与小说/角色保持一致的交互模式，能减少学习成本和误操作。

## 阶段 11.1：统一提示机制（角色模块，已完成）

日期：2026-05-17

### 目标
将“小说角色”创建/编辑/删除提示统一为页面级 toast 机制，与章节模块一致。

### 本阶段完成
- `CharacterManager` 移除组件内部成功/失败 `Alert`，改为通过回调上抛提示事件。
- `NovelsPage` 增加统一提示入口：
  - `notifySuccess(text)`：左下角成功提示，自动消失
  - `notifyError(text)`：左下角错误提示，自动消失
- 角色操作提示统一接入：
  - 创建角色成功/失败
  - 编辑角色成功/失败
  - 删除角色成功/失败
- 保持原有交互不变：
  - 新增/编辑成功后返回“小说角色”主视图并刷新列表。

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 提示逻辑应集中在页面层统一管理，业务组件专注表单与提交。
- 避免将后端原始错误直接暴露给用户，前端应转换为可读提示文案。

## 阶段 11.2：角色重要度与删除保护（已完成）

日期：2026-05-17

### 目标
为人物卡增加重要度（`importance_level`），并在重要度较高时阻止删除误操作。

### 本阶段完成
- 新增 migration：
  - `backend/migrations/202605170005_add_importance_level_to_characters.up.sql`
  - `backend/migrations/202605170005_add_importance_level_to_characters.down.sql`
- 角色模型新增字段：`importance_level`（0-9）。
- 后端创建/更新角色增加范围校验：`importance_level` 必须在 `0-9`。
- 后端删除保护：
  - 当 `importance_level >= 7` 时，删除接口拒绝删除并返回业务错误。
- 前端人物管理新增输入项：
  - `Importance Level (0-9, 7+ protected)`
- 角色列表增加重要度显示：
  - `Importance {n}`
- 前端错误文案映射新增：
  - 删除受保护角色时提示：`该人物为主要角色，禁止删除。请先降低权重后再删除。`

### 运行与验证
- 通过：`go test ./...`（backend）。
- 通过：`npm run build`（frontend）。

### 复习要点
- 防误删需要后端硬约束，不能只依赖前端按钮状态。
- 重要度阈值规则明确后（当前为 `>= 7`），前后端必须同步实现并统一提示文案。

## 阶段 11.3：章节字数自动同步计算（已完成）

日期：2026-05-17

### 目标
将章节 `word_count` 改为自动计算，避免手填误差；空白字符不计入字数。

### 本阶段完成
- 前端章节编辑区：
  - `Word Count` 改为只读展示。
  - 基于 `Body` 实时自动计算。
  - 计算规则：去除空白字符后统计长度（空格/换行/Tab 不计入）。
- 后端章节用例：
  - 在创建/更新章节时，不再信任前端传入值。
  - 统一根据 `Body` 重新计算 `word_count` 后保存。

### 运行与验证
- 通过：`go test ./...`（backend）。
- 通过：`npm run build`（frontend）。

### 复习要点
- 字数这类可推导字段应由系统自动计算，前端仅展示，后端做最终兜底。

## 阶段 11.4：章节独立写作页与布局优化（已完成）

日期：2026-05-17

### 目标
将章节编辑从列表页中拆出，改为独立写作页面，并优化长文本编辑体验。

### 本阶段完成
- 新增独立页面：`frontend/src/pages/ChapterEditorPage.tsx`。
- 交互改造：
  - 章节列表页仅保留列表与入口。
  - 点击“新增章节/编辑章节”进入独立写作页。
- 写作页布局优化：
  - 正文主编辑区居中展示。
  - 正文输入区加大（适配长章节写作）。
  - 小说名与页面标题居中显示。
  - 右侧按钮独立栏位，不覆盖正文输入区。
  - 右侧“章节总结/章节大纲/生成指令”使用同一面板位置切换显示。
  - 支持再次点击按钮或点击面板 `X` 关闭侧栏。

### 运行与验证
- 通过：`npm run build`（frontend，多次构建通过）。

### 复习要点
- 列表页与写作页职责分离后，后续扩展（AI 辅助、草稿、版本）会更稳定。
- 长文本编辑优先保证“可写性”（空间、聚焦、操作路径），再做装饰性美化。

## 阶段 11.5：章节列表与写作体验收尾（已完成）

日期：2026-05-17

### 目标
优化章节列表可用性与章节写作流程稳定性。

### 本阶段完成
- 章节列表增强：
  - 搜索（按章节号/标题）
  - 排序（章节号新到旧、章节号旧到新、最近更新）
- 列表状态信息增强：
  - 字数
  - 是否有总结
  - 最近更新时间
- 删除确认增强：
  - 删除弹窗显示章节号与标题上下文，降低误删风险。
- 写作页操作增强：
  - 新增底部固定操作栏（返回章节列表 / 保存章节），长文滚动时也可随时操作。
- 自动草稿（本地）增强：
  - 编辑内容自动保存到 localStorage。
  - 再次进入同一章节（或新建页）可自动恢复草稿。
  - 保存成功后自动清除对应草稿。

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 长文本编辑应优先保证“可恢复性”和“可达操作”。
- 列表页的检索与排序先到位，可显著降低后续章节增多后的管理成本。

## 阶段 11.6：写作页视觉与交互微调（已完成）

日期：2026-05-17

### 目标
优化章节写作页的可读性与操作手感，减少写作时视觉干扰。

### 本阶段完成
- 写作页 Header 重构为更轻量的信息栏样式（左侧返回、中心信息、右侧操作）。
- 主按钮统一为圆角胶囊风格，强化主次操作层级。
- 右侧功能按钮（总结/大纲/指令）视觉统一并强化选中态反馈。
- 右侧面板支持再次点击按钮或点击 `X` 关闭。

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 长文本场景中，Header 与按钮优先服务“低干扰+高可达”，避免喧宾夺主。

## 阶段 11.7：写作页布局修正（已完成）

日期：2026-05-17

### 目标
修正正文区与侧栏交互后的空间分配问题，保证主编辑区视觉稳定。

### 本阶段完成
- 正文编辑区收窄并居中，避免过宽造成阅读与输入疲劳。
- 右侧按钮改为独立栏位，不再覆盖或压住正文输入区域。
- 侧栏弹出后保持“正文主导、侧栏辅助”的布局关系。

### 运行与验证
- 通过：`npm run build`（frontend）。

### 复习要点
- 任何侧栏增强都不应破坏正文主路径，编辑区稳定性优先于功能堆叠。

## 阶段 11.8：章节正文缩进输入行为修复（已完成）

日期：2026-05-17

### 目标
稳定正文两字缩进规则，修复退格与粘贴/复制导致的编辑异常。

### 本阶段完成
- 缩进规则：
  - 首行默认两个全角空格缩进。
  - 回车后新行自动带两个全角空格缩进。
- 退格行为修复：
  - 在非首行缩进区域按 `Backspace` 可正确合并到上一行。
  - 避免光标异常跳转与“卡住”问题。
- 粘贴行为修复：
  - 保留“不能粘贴到缩进前”的位置保护。
  - 粘贴内容不再被强行改写为每行补缩进。
- 复制/剪切行为修复：
  - 复制或剪切到剪贴板时，自动去除每行前两个缩进字符，避免外部文本污染。

### 运行与验证
- 通过：`npm run build`（frontend，多次构建通过）。

### 复习要点
- 文本编辑器类交互要优先保证“光标稳定”和“用户输入原样可控”。
- 约束应放在“位置保护”而非“内容强改写”，可显著降低副作用。

## 阶段 12：分卷命名必填与章节创建约束（已完成）

日期：2026-05-17

### 目标
确保分卷具备可识别名称，并强制“先有分卷、后有章节”的创建顺序。

### 本阶段完成
- 后端校验增强：
  - 分卷创建/更新时 `title` 改为必填，空值直接返回错误（不再自动填“未命名分卷”）。
- 前端交互约束：
  - 新建分卷输入框改为必填语义（去掉“可选”）。
  - 卷名为空时禁用“新建分卷”按钮，并在提交时提示错误。
  - 无分卷时禁用“新增章节”按钮，并提示“请先创建分卷（必须填写卷名）”。
- 展示文案统一：
  - 分卷显示改为 `第X卷：卷名`（章节列表与章节编辑页下拉统一）。

### 运行与验证
- 通过：`go test ./...`（backend）
- 通过：`npm run build`（frontend）

### 复习要点
- 结构化写作流程中，分卷是章节的上层实体，UI 与 API 都应强制表达该约束。
- 业务主数据（如卷名）应尽量避免“自动兜底占位”，优先要求显式输入。

## 阶段 12.1：分卷改名替代删除（已完成）

日期：2026-05-17

### 目标
降低分卷误操作风险：支持修改卷名，不提供删除分卷的前端入口。

### 本阶段完成
- 分卷管理新增“改名”能力：
  - 在章节页每个分卷标题右侧新增编辑按钮。
  - 点击后弹出“编辑卷名”对话框，卷名必填，保存后刷新分卷列表。
- API 补充：
  - 新增前端 `updateVolume` 调用（`PUT /novels/:novelId/volumes/:id`）。
- 风险控制：
  - 前端仍不提供分卷删除入口，避免误删对章节结构造成影响。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 对承载结构关系的数据（如分卷），优先提供“可逆操作（改名）”，弱化“不可逆操作（删除）”。

## 阶段 12.3：新增章节默认分卷优化（已完成）

日期：2026-05-17

### 目标
让“新增章节”默认落在最近分卷，减少每次手动切换分卷的操作成本。

### 本阶段完成
- 章节编辑页默认分卷策略调整：
  - 新增章节时，默认选中“卷号最大”的分卷（最近卷）。
  - 若卷号相同，则取 `id` 更大的分卷作为最近卷。
- 编辑章节保持原逻辑：
  - 编辑已有章节时，仍沿用该章节原有分卷。
- 异常兜底：
  - 当当前分卷无效时，新增章节回落到最近卷；编辑章节回落到可用分卷。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 默认值应贴合最高频用户路径；写作场景里“续写当前最新卷”通常是更自然的默认行为。

## 阶段 12.4：分卷管理完善（已完成）

日期：2026-05-17

### 目标
补全分卷与章节之间的日常管理效率：可见、可改、可快速调整。

### 本阶段完成
- 分卷信息增强：
  - 分卷标题显示章节数，格式为 `第X卷：卷名（N章）`。
- 章节转卷快捷操作：
  - 在章节列表每条章节右侧新增分卷下拉，可直接把章节移动到其他分卷。
  - 移动完成后自动刷新章节列表并提示成功。
- 操作同步与保护：
  - 移动中的章节禁用编辑/删除/再次移动，避免并发误操作。
  - 分卷删除入口依旧不提供，保持安全策略。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 列表页上的“就地编辑”能显著提升结构维护效率，尤其适合长篇写作中频繁重排章节归属的场景。

## 阶段 13.0：小说 Markdown 导出（已完成）

日期：2026-05-17

### 目标
提供 MVP 必需的 Markdown 导出能力，支持按小说维度完整导出写作内容。

### 本阶段完成
- 后端新增导出接口：
  - `GET /novels/:novelId/export/markdown`
  - 校验登录用户对小说的访问权限后导出。
- 导出结构实现：
  - 顶层为小说标题。
  - 按分卷顺序输出 `第X卷：卷名`。
  - 每卷内按章节号输出 `第N章 标题` 与正文。
- 响应下载头：
  - `Content-Type: text/markdown`
  - `Content-Disposition: attachment; filename="小说名-YYYYMMDD.md"`
- 前端新增导出按钮：
  - 在“小说章节”页增加“导出 Markdown”按钮。
  - 点击后自动下载文件，并复用统一提示反馈成功/失败。

### 运行与验证
- 通过：`go test ./...`（backend）
- 通过：`npm run build`（frontend）

### 复习要点
- 导出能力优先保证“可备份、可迁移、可离线保存”，格式简单稳定比复杂样式更关键。

## 阶段 13.1：导出内容与范围可选（已完成）

日期：2026-05-17

### 目标
让导出从“整本固定导出”升级为“可配置导出”，支持内容粒度和范围控制。

### 本阶段完成
- 后端新增可扩展导出接口：
  - `POST /novels/:novelId/export`
  - 参数支持：`format`（当前 markdown）、`scope`（all / volume / chapter_range）、`volume_id`、`from_chapter`、`to_chapter`。
- 导出内容可选：
  - 支持勾选导出 `正文`、`章节总结`、`章节大纲`。
  - 至少需要选择一种内容，后端会校验。
- 导出范围可选：
  - 全部章节
  - 按分卷
  - 按章节区间
- 前端导出入口升级：
  - 导出按钮改为“导出设置”弹窗。
  - 弹窗内可选择范围与内容，确认后下载。
- 文件名兼容保持：
  - 继续使用 UTF-8 `filename*`，避免中文文件名乱码。

### 运行与验证
- 通过：`go test ./...`（backend）
- 通过：`npm run build`（frontend）

### 复习要点
- 导出功能应优先服务“内容资产管理”，可配置范围与字段比固定模板更贴近真实写作工作流。

## 阶段 14.0：AI 章节生成最小闭环（已完成）

日期：2026-05-17

### 目标
完成 MVP 最后一块核心链路：结构化指令 -> OpenAI 生成 -> 回填编辑页。

### 本阶段完成
- 后端新增 AI 生成接口：
  - `POST /novels/:novelId/chapters/generate`
  - 输入：`volume_id`、`chapter_number`、`title`、`generation_instruction`
  - 输出：`outline`、`body`、`summary`
- OpenAI 服务接入：
  - 新增 `internal/service/openai_client.go`，调用 `/chat/completions`
  - 强制 JSON 输出并解析为结构化字段。
  - 若 JSON 解析失败或字段缺失，返回错误（不保存章节）。
- 生成上下文组装：
  - 读取小说设定、主要人物（importance >= 5）、最近章节总结（按配置最近章数）。
  - 构造成结构化提示词发送给 OpenAI。
- 前端写作页接入：
  - 新增“AI 生成”按钮。
  - 成功后自动回填正文/大纲/总结到编辑区，供用户继续修改后保存。
- 配置扩展：
  - 新增环境变量：`OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`。

### 运行与验证
- 通过：`go test ./...`（backend）
- 通过：`npm run build`（frontend）

### 复习要点
- MVP 中“生成”和“保存”应保持分离：先生成可编辑草稿，再由用户确认保存，能显著降低错误内容入库风险。

## 阶段 14.1：AI 生成指定登场人物（已完成）

日期：2026-05-17

### 目标
降低 AI 生成人物噪音：由用户显式指定本章登场人物，模型只读取相关人物卡。

### 本阶段完成
- 生成接口扩展：
  - `POST /novels/:novelId/chapters/generate` 新增 `character_ids` 入参。
- 后端人物选择策略：
  - 若传入 `character_ids`，仅使用这些人物卡参与生成提示。
  - 若未传入，回退为原先的“主要人物（importance >= 5）”。
  - 对非法/不存在的 `character_ids` 返回错误。
- 前端写作页增强：
  - 在“生成指令”面板新增“本章登场人物（可选）”多选。
  - 选中结果随 AI 生成请求一起提交。
  - 选择会进入本地草稿保存并自动恢复。
- 数据加载联动：
  - 章节页进入/切换小说时，会同步拉取人物列表供多选使用。

### 运行与验证
- 通过：`go test ./...`（backend）
- 通过：`npm run build`（frontend）

### 复习要点
- 生成输入越结构化、越贴近当前章节任务，越能减少“角色乱入”和风格漂移。

## 阶段 14.2：登场人物选择可搜索与分组（已完成）

日期：2026-05-18

### 目标
解决人物规模增大后的选择效率问题，避免在几百人物中手动滚动查找。

### 本阶段完成
- 选择器升级：
  - 将“本章登场人物”从普通多选下拉升级为可搜索多选（Autocomplete）。
  - 支持按 `姓名 / 别名 / 身份` 关键词即时过滤。
- 分组展示：
  - 选项按 `最近使用 / 主要人物 / 全部人物` 分组显示。
- 最近使用机制：
  - 成功 AI 生成后，记录本次所选人物到本地最近使用列表（每本小说独立保存）。
  - 下次选择时优先在“最近使用”分组显示，减少重复检索成本。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 当数据规模扩大时，优先做“检索+分组+最近使用”比单纯滚动列表更能提升可用性。

## 阶段 15.0：前端结构重构（不改功能，已完成）

日期：2026-05-19

### 目标
在不改变任何业务功能与交互行为的前提下，降低超大页面文件复杂度，并抽取可复用逻辑。

### 本阶段完成
- 拆分 `NovelsPage` 重型内联模块：
  - 新增 `frontend/src/components/novels/NovelForm.tsx`
  - 新增 `frontend/src/components/novels/ExportDialog.tsx`
  - `NovelsPage.tsx` 改为组合调用，移除页面底部冗长内联定义。
- 抽取 `ChapterEditorPage` 重复操作逻辑：
  - 新增 `frontend/src/components/chapter-editor/EditorActionButtons.tsx`
  - 顶部与底部操作栏复用同一组件，避免重复维护。
- 代码复用改进：
  - 将按钮禁用逻辑、按钮样式与动作绑定集中到复用组件，减少后续改动扩散风险。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 重构应优先抽离“重复 UI + 重复条件逻辑”，在不改功能的情况下快速提升可维护性。

## 阶段 15.1：ChapterEditor 深拆分（不改功能，已完成）

日期：2026-05-19

### 目标
继续降低页面单文件复杂度，把章节编辑页的状态逻辑与可复用子模块拆出。

### 本阶段完成
- 新增 `frontend/src/hooks/useChapterEditor.ts`：
  - 集中管理章节编辑页的状态、API 调用、自动保存与交互动作。
- 新增 `frontend/src/components/chapter-editor/CharacterPicker.tsx`：
  - 抽离“本章登场人物”选择 UI，减少主页面职责。
- `frontend/src/pages/ChapterEditorPage.tsx`：
  - 改为组合 hook + 子组件，显著减少页面内联逻辑。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 页面文件应优先保留“编排职责”，复杂状态与领域交互放到 hook，提升后续迭代稳定性。

## 阶段 15.2：NovelsPage 深拆分（不改功能，已完成）

日期：2026-05-19

### 目标
解决 `NovelsPage.tsx` 过长问题，把侧栏、头部与三大业务区块拆为独立组件，降低维护成本。

### 本阶段完成
- 新增组件：
  - `frontend/src/components/novels/NovelSidebar.tsx`
  - `frontend/src/components/novels/NovelTopCard.tsx`
  - `frontend/src/components/novels/NovelDetailSection.tsx`
  - `frontend/src/components/novels/NovelCharactersSection.tsx`
  - `frontend/src/components/novels/NovelChaptersSection.tsx`
- 调整 `frontend/src/pages/NovelsPage.tsx`：
  - 保留页面级状态与数据流。
  - 渲染层改为组合上述组件。
  - 原有功能、交互入口、弹窗与提示逻辑保持一致。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 当页面超过千行时，优先按“导航层 / 区块层 / 通用层”拆分，再逐步下沉状态逻辑，风险最低。

## 阶段 15.3：NovelsPage 状态下沉到 Hook（不改功能，已完成）

日期：2026-05-19

### 目标
继续在不改行为前提下，降低 `NovelsPage` 页面复杂度，把大量 `useState/useEffect` 与业务动作从页面中下沉。

### 本阶段完成
- 新增 `frontend/src/hooks/useNovelsPage.ts`：
  - 承接原 `NovelsPage` 的页面状态、派生数据、副作用与动作函数。
  - 保持原有 API 调用与交互流程不变。
- 精简 `frontend/src/pages/NovelsPage.tsx`：
  - 仅负责视图编排与将 state/actions 传给子组件。
  - 删除页面内大段状态与数据处理逻辑。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 先“组件拆分”，再“状态下沉”是低风险重构路径；页面应尽量只承担编排职责。

## 阶段 15.5：ChapterEditorPage 状态下沉到 Hook（不改功能，已完成）

日期：2026-05-19

### 目标
让章节编辑页与 novels 页保持一致：页面负责编排，状态与业务动作下沉到 hook。

### 本阶段完成
- 新增 `frontend/src/hooks/useChapterEditor.ts`：
  - 承接章节编辑页的状态、副作用、本地草稿、正文缩进规则、保存与 AI 生成逻辑。
- 精简 `frontend/src/pages/ChapterEditorPage.tsx`：
  - 改为主要负责 UI 结构与 props 传递。
  - 交互行为保持不变。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 页面复杂度下降后，后续做编辑器功能扩展（如快捷键、草稿策略）时，可在 hook 内集中改动，回归风险更低。

## 阶段 16.0：后端基础测试（已完成）

日期：2026-05-19

### 目标
为 MVP 当前后端能力补充最小可运行测试，先覆盖基础配置、鉴权中间件、路由健康检查与常用参数解析。

### 本阶段完成
- 新增配置测试：
  - `backend/internal/config/config_test.go`
  - 覆盖默认值加载与环境变量覆盖。
- 新增鉴权中间件测试：
  - `backend/internal/middleware/auth_test.go`
  - 覆盖缺失 Bearer token 与合法 token 注入 `user_id`。
- 新增 handler 辅助逻辑测试：
  - `backend/internal/handler/novel_handler_test.go`
  - 覆盖 `parseIDParam` 对合法/非法 id 的处理。
- 新增 API 启动骨架测试：
  - `backend/cmd/api/main_test.go`
  - 覆盖 `/health` 路由可用性。

### 运行与验证
- 通过：`go test ./...`（backend）

### 复习要点
- 先补“高频基础能力”的测试（配置、鉴权、健康检查、参数校验），能在低成本下显著降低后续迭代回归风险。

## 阶段 16.1：后端业务冒烟测试（已完成）

日期：2026-05-19

### 目标
在基础测试之上，补充 MVP 业务层最常见的错误路径与鉴权入口检查，形成最小回归网。

### 本阶段完成
- 新增 usecase 输入校验冒烟测试：
  - `backend/internal/usecase/validation_smoke_test.go`
  - 覆盖 Auth/Novel/Character/Volume/Chapter 的核心入参校验。
  - 覆盖章节字数计算函数 `countNonSpaceChars`。
- 新增受保护路由未登录测试：
  - `backend/cmd/api/authz_smoke_test.go`
  - 覆盖 `/auth/me` 与 `/novels` 在未带 Bearer token 时返回 `401`。

### 运行与验证
- 通过：`go test ./...`（backend）

### 复习要点
- 先用“输入校验 + 鉴权入口”的冒烟测试兜底，再逐步补充数据库依赖的 happy path 集成测试，迭代成本更可控。

## 阶段 16.2：数据库集成级最小 CRUD 测试（已完成）

日期：2026-05-19

### 目标
在真实 MySQL 上验证 repository 层最小闭环，覆盖用户、小说、分卷、章节的核心 CRUD 与用户隔离。

### 本阶段完成
- 新增集成测试：
  - `backend/internal/repository/repository_integration_test.go`
- 覆盖流程：
  - 创建用户（2 个）
  - 创建小说并按 `user_id` 列表查询
  - 跨用户读取返回 `sql.ErrNoRows`（权限隔离）
  - 更新小说
  - 创建分卷
  - 创建/查询/更新/删除章节
  - 删除分卷与小说
- 测试特性：
  - MySQL 不可达时自动 `Skip`（避免阻断无数据库环境）
  - 测试前后自动清理数据，保证可重复执行

### 运行与验证
- 通过：`go test ./internal/repository -run TestRepositoryIntegration_MinimalCRUD -v`
- 通过：`go test ./...`（backend）

### 复习要点
- 先做 repository 级集成测试，可以在不拉高 HTTP 复杂度的前提下，尽早发现外键、权限过滤、SQL 更新路径的问题。

## 阶段 16.3：测试安全护栏（已完成）

日期：2026-05-19

### 目标
避免测试误操作真实开发库，建立“默认安全、显式开启集成测试”的执行规范。

### 本阶段完成
- 新增集成测试安全守卫工具：
  - `backend/internal/repository/integration_guard_test.go`
  - 要求：
    - `INTEGRATION_TEST=1` 才允许运行集成测试
    - `MYSQL_DSN` 必须包含 `_test`（强制测试库）
- 更新 README 测试规范：
  - 增加“默认安全测试（不触库）”命令
  - 增加“集成测试必须显式开启 + 测试库命名约束”

### 运行与验证
- 通过：`go test ./...`（backend）

### 复习要点
- 真正安全的测试策略不是“靠记忆避免误跑”，而是“默认不危险 + 规则硬约束”。

## 阶段 16.4：后端 API 最小集成测试（测试库）

日期：2026-05-19

### 目标
在不触碰开发主库的前提下，验证核心 API 闭环可用性（Auth + Novels）。

### 本阶段完成
- 新增：`backend/cmd/api/api_integration_test.go`
- 测试覆盖：
  - `POST /auth/register`
  - `POST /auth/login`
  - `GET /auth/me`
  - `POST /novels`
  - `GET /novels`
- 安全机制：
  - 必须 `INTEGRATION_TEST=1`
  - 必须 `MYSQL_DSN` 包含 `_test`
  - 仅清理 `it_` 前缀测试数据，不清空整表

### 运行与验证
- 通过：`INTEGRATION_TEST=1 MYSQL_DSN='novel:novel@tcp(127.0.0.1:3306)/novel_agent_test?parseTime=true&charset=utf8mb4,utf8' go test ./cmd/api -run TestAPIIntegration_AuthAndNovelsFlow -v`
- 通过：`go test ./...`（backend）

### 复习要点
- 集成测试应以“显式开关 + 专用测试库 + 定向清理”三件套为基线，避免再次误伤开发数据。

## 阶段 16.5：API 集成测试扩展到 Characters/Chapters CRUD（已完成）

日期：2026-05-19

### 目标
将 API 集成测试从 Auth+Novels 扩展到 Characters/Chapters，形成更完整的 MVP 回归链路。

### 本阶段完成
- 扩展 `backend/cmd/api/api_integration_test.go`：
  - 在现有 `register/login/me/novels` 流程上继续覆盖：
    - `POST /novels/:id/volumes`
    - `POST/GET/PUT/DELETE /novels/:id/characters`
    - `POST/GET/PUT/DELETE /novels/:id/chapters`
- 保持测试安全策略不变：
  - `INTEGRATION_TEST=1`
  - `MYSQL_DSN` 必须包含 `_test`
  - 仅清理 `it_` 前缀测试数据

### 运行与验证
- 通过：`go test ./...`（backend）
- 通过：`INTEGRATION_TEST=1 MYSQL_DSN='.../novel_agent_test...' go test ./cmd/api -run TestAPIIntegration_AuthAndNovelsFlow -v`

### 复习要点
- 先用一条“最小完整链路”覆盖关键资源，比一开始做大量分散 case 更快建立稳定回归信心。

## V1

### 阶段 V1-1：Lore Entries 后端最小闭环（已完成）

日期：2026-05-19

### 目标
实现“世界观设定卡（Lore Entries）”后端最小 CRUD，并支持与人物多对多绑定（可空、可单人、可多人）。

### 本阶段完成
- 新增领域模型：`backend/internal/domain/lore_entry.go`
- 新增仓储层：`backend/internal/repository/lore_entry_repository.go`
  - 支持 `Create/List/Get/Update/Delete`
  - 支持 `character_ids` 多对多绑定写入与读取
  - 校验 `character_ids` 必须属于当前小说且受当前用户权限约束
- 新增用例层：`backend/internal/usecase/lore_entry_usecase.go`
  - 校验 `category/name/description` 必填
  - 校验 `category` 取值：`artifact/elixir/formation/technique/location/organization/other`
- 新增处理层：`backend/internal/handler/lore_entry_handler.go`
- 路由接入（JWT 保护）：
  - `POST /novels/:novelId/lore-entries`
  - `GET /novels/:novelId/lore-entries`
  - `GET /novels/:novelId/lore-entries/:id`
  - `PUT /novels/:novelId/lore-entries/:id`
  - `DELETE /novels/:novelId/lore-entries/:id`

### 运行与验证
- 通过：`go test ./...`（backend）

### 复习要点
- 复杂设定（法器/丹药/阵法等）先统一抽象为一张设定卡主表，再通过关联表绑定人物，是 MVP 后续扩展最稳妥的起点。

### 阶段 V1-2：Lore Entries 前端最小闭环（已完成）

日期：2026-05-19

### 目标
实现设定卡前端最小管理闭环，并接入现有小说导航结构。

### 本阶段完成
- 新增 API：`frontend/src/api/loreEntries.ts`
- 新增组件：
  - `frontend/src/components/novels/LoreEntryManager.tsx`
  - `frontend/src/components/novels/NovelLoreEntriesSection.tsx`
- 左侧导航新增“小说设定”子模块。
- 页面支持：设定卡列表、创建、编辑、删除（二次确认）。
- 设定卡支持人物多选绑定（可为空）。
- 顶部标题区已支持“小说设定”状态展示。

### 运行与验证
- 通过：`npm run build`（frontend）

### 复习要点
- 复杂设定先做“统一设定卡 + 人物绑定”可以快速覆盖修仙类核心整理需求，后续再细分专表。

### 阶段 V1-3：章节生成接入设定卡（已完成）

日期：2026-05-20

### 目标
让 AI 章节生成可同时参考“人物 + 设定卡”，提升修仙题材等复杂世界观的可控性。

### 本阶段完成
- 后端：`POST /novels/:novelId/chapters/generate` 新增 `lore_entry_ids` 入参。
- 后端：生成用例按所选 `lore_entry_ids` 查询并校验归属后，注入 prompt。
- 前端：写作页“生成指令”面板新增“本章相关设定（可选）”多选。
- 前端：生成请求会同时提交 `character_ids + lore_entry_ids`。

### 运行与验证
- 通过：`cd backend && go test ./...`
- 通过：`cd frontend && npm run build`

### 复习要点
- 生成链路的上下文应由作者显式选择，不应默认塞入全部设定，避免 prompt 噪声过大。

### 阶段 V1-4：小说工作区导航重构（已完成）

日期：2026-05-20

### 目标
减少“小说详情/角色/设定/章节”四个模块中的重复选书步骤，统一为“先选小说，再切模块”。

### 本阶段完成
- 左侧 `我的小说` 改为直接展示小说列表并支持选中。
- 右侧新增横向 Tab：`小说详情 / 小说角色 / 小说设定 / 小说章节`。
- 四个模块内移除“选择小说”下拉，统一使用当前选中小说上下文。
- 小说详情模块改为“当前小说信息 + 编辑入口”。

### 运行与验证
- 通过：`cd frontend && npm run build`

### 复习要点
- 将“资源选择（小说）”与“功能切换（模块）”分层，是降低界面复杂度和操作负担的关键。

### 阶段 V1-5：生成失败路径集成测试补强（已完成）

日期：2026-05-20

### 目标
补齐 `chapters/generate` 关键失败路径，降低后续生成链路回归风险。

### 本阶段完成
- 扩展 `backend/cmd/api/api_integration_test.go` 的失败路径覆盖：
  - `generation_instruction` 缺失
  - 非法 `character_ids`
  - 非法 `lore_entry_ids`
  - 跨用户越权访问同一 novel 的生成接口
- 更新测试 schema 校验，加入：
  - `lore_entries`
  - `lore_entry_characters`
- 更新测试清理逻辑，增加 lore 相关测试数据清理。

### 运行与验证
- 通过：`INTEGRATION_TEST=1 MYSQL_DSN='novel:novel@tcp(127.0.0.1:3306)/novel_agent_test?parseTime=true&charset=utf8mb4,utf8' go test ./cmd/api -run TestAPIIntegration_FailurePaths -v`
- 通过：`go test ./...`（backend）

### 复习要点
- 生成接口最容易在“参数组合 + 权限边界”处回归，优先补这类失败路径测试能显著提升稳定性。

### 阶段 V1-6：前端生成交互最小测试（已完成）

日期：2026-05-20

### 目标
为章节生成关键交互补充前端最小自动化测试，降低回归风险。

### 本阶段完成
- 引入前端测试工具链：Vitest + Testing Library + jsdom。
- 新增测试文件：`frontend/src/hooks/useChapterEditor.test.tsx`。
- 覆盖用例：
  - 生成失败后显示错误并允许重试
  - 保存失败不会保留生成重试状态
  - 生成请求携带 `character_ids + lore_entry_ids`
  - 编辑模式保存调用 `updateChapter`

### 运行与验证
- 通过：`cd frontend && npm run test`
- 通过：`cd frontend && npm run build`

### 复习要点
- 先覆盖“生成失败/重试/参数携带”这类高风险链路，再逐步扩展到页面级测试，是 MVP 后阶段最稳妥的测试策略。

## V1-5（后半）：章节生成质量控制参数（已完成）

日期：2026-05-20

### 目标
为 AI 章节生成增加“可控性”参数，降低翻译腔、提升生成稳定性与长度可预期性。

### 本阶段完成
- 后端 `chapters/generate` 新增请求参数：
  - `target_word_min`
  - `target_word_max`
  - `avoid_translation_tone`
  - `avoid_modern_slang`
  - `keep_pov_consistent`
  - `keep_tense_consistent`
- 后端生成用例新增参数校验：
  - 目标字数范围不允许负数。
  - 下限大于上限时拒绝请求。
  - 过大范围（>12000）拒绝请求。
- prompt 规则增强：
  - 明确要求输出仅为 JSON。
  - 明确 `body/outline/summary` 类型要求。
  - 注入目标字数范围与风格硬约束条目。
- 前端写作页“指令”面板新增：
  - 目标字数下限/上限输入。
  - 4 个风格约束开关。
  - 范围无效时前端直接阻止生成并提示。
- 前端本地草稿已持久化上述新参数。

### 运行与验证
- 已运行：
  - `cd backend && go test ./...`
  - `cd frontend && npm run build`

### 复习要点
- 质量控制先做“可配置硬约束”，再做更精细的模板策略。
- 保持“生成与保存分离”，用户确认后再落库。

## V1-6（文档增量）：AI 生成调参指南（已完成）

日期：2026-05-20

### 目标
把“章节生成质量参数”变成可直接执行的写作手册，降低试错成本。

### 本阶段完成
- 新增 `docs/ai-generation-guide.md`：
  - 修仙主线/战斗/过渡/情绪章的推荐参数组合
  - 可直接复用的生成指令模板
  - 常见质量问题排查（太短、空泛、翻译感、人设跑偏）
- README 增加该指南入口。
- V1 设计书与 Roadmap 同步记录此增量。

### 复习要点
- 先用默认推荐组合生成，再按“问题类型”单变量调参，不要一次改很多项。

## V1-6（交互重构）：最近章节参考数迁移到写作页（已完成）

日期：2026-05-20

### 目标
把“最近章节参考数”从小说详情迁移到章节编辑/生成上下文，降低跨页面调参成本。

### 本阶段完成
- 前端：
  - 从小说详情编辑表单移除“最近章节参考数量”输入。
  - 在章节写作页“生成指令”面板新增“最近章节参考数”输入。
  - 该值参与本地草稿持久化。
- 后端：
  - `POST /novels/:novelId/chapters/generate` 支持 `recent_chapter_count`。
  - 生成逻辑改为：本次请求值优先；未提供/<=0 时回退小说默认值。
- 测试与构建：后端测试和前端构建通过。

### 复习要点
- 生成参数尽量收敛到写作页，避免在“小说详情”和“写作页”来回切换。

## V1-6（文档增量）：环境分层模板（已完成）

日期：2026-05-20

### 目标
为 dev/test 使用场景提供稳定可复用的环境变量模板，降低误用主库风险。

### 本阶段完成
- 新增模板文件：
  - `.env.example`
  - `.env.dev.example`
  - `.env.test.example`
- README 增加环境分层说明：
  - dev/test/prod 职责边界
  - `.env.dev` 与 `.env.test` 加载示例
- 明确约束：
  - test 环境必须使用 `_test` 库
  - 集成测试需 `INTEGRATION_TEST=1`

### 复习要点
- 当前只落地 dev/test 实用配置，prod 先保留文档占位，避免过早引入部署复杂度。

## V1-6（可观测性第一步）：生成 token 与日志（已完成）

日期：2026-05-20

### 目标
让章节生成过程具备基础可观测性，便于定位慢请求与失败原因，并让前端感知本次 token 消耗。

### 本阶段完成
- 后端 OpenAI 生成链路新增输出：
  - `model`
  - `usage.prompt_tokens`
  - `usage.completion_tokens`
  - `usage.total_tokens`
- 后端章节生成 handler 新增日志：
  - 成功日志：`user_id/novel_id/latency_ms/model/tokens`
  - 失败日志：`user_id/novel_id/latency_ms/err`
- 前端生成成功提示新增 token 信息（若返回 `total_tokens`）。

### 运行与验证
- `cd backend && go test ./...`
- `cd frontend && npm run build`

### 复习要点
- 这一步先做“观测”，不做计费统计与持久化；后续若做成本分析再落库。

## V1-6（交互增强）：生成失败提示分级（已完成）

日期：2026-05-21

### 目标
把章节生成失败提示从通用错误改为可执行建议，降低试错成本。

### 本阶段完成
- 前端生成错误映射：
  - `ai output parse failed` -> 模型输出格式异常（建议重试/简化指令）
  - `ai service request failed` / 401/403/429 -> 检查 Key、额度、模型配置
  - timeout / network / failed to fetch -> 建议缩短字数后重试
- 新增 hook 测试：覆盖“解析失败提示映射”。

### 运行与验证
- `cd frontend && npm run test -- --run`
- `cd frontend && npm run build`

### 复习要点
- 失败提示不仅要说明“失败”，还要告诉用户“下一步怎么做”。

## V1-6（协议增强）：标准错误码 + 前端按码映射（已完成）

日期：2026-05-21

### 目标
让章节生成失败提示基于稳定协议（错误码）而不是纯文本匹配，提升长期可维护性。

### 本阶段完成
- 后端 `POST /novels/:novelId/chapters/generate` 错误响应统一为：
  - `code`
  - `error`
- 前端新增 `APIError`，并在生成接口中抛出带 `status/code/message` 的异常。
- 章节编辑页错误提示改为：
  - 优先按 `code` 映射
  - 文本关键字匹配作为兜底

### 运行与验证
- `cd backend && go test ./...`
- `cd frontend && npm run test -- --run`
- `cd frontend && npm run build`

### 复习要点
- 错误码是“协议层”，文案是“展示层”，两者分离后更稳定。

## V1-7.1：本地生成历史（已完成）

日期：2026-05-21

### 目标
在不改后端与数据库的前提下，让章节生成结果支持“最近版本回看与回填”。

### 本阶段完成
- 前端章节写作页新增“历史”侧栏按钮。
- 按“小说 + 章节”维度在 `localStorage` 保存最近 3 条生成记录。
- 记录内容包括：
  - 生成时间
  - `outline/body/summary`
  - `model`
  - `total_tokens`
  - 指令摘要
- 支持点击“回填此版本”一键回填到当前编辑器。

### 运行与验证
- `cd frontend && npm run test -- --run`
- `cd frontend && npm run build`

### 复习要点
- 这一步只做本地历史，不做后端持久化；后续可在 V1-7.2 做版本对比与防误覆盖。

## V1-7.2：历史回填防误覆盖（已完成）

日期：2026-05-21

### 目标
避免误点“回填历史”后直接覆盖当前写作内容。

### 本阶段完成
- 历史记录点击“回填此版本”后，不再直接覆盖。
- 新增确认弹窗，显示：
  - 当前内容预览（正文/大纲/总结）
  - 历史版本预览（正文/大纲/总结 + 时间/模型/token）
- 只有用户点击“确认回填”才会真正回填。

### 运行与验证
- `cd frontend && npm run test -- --run`
- `cd frontend && npm run build`

### 复习要点
- “可恢复”功能一定要配“防误触”确认，尤其是会覆盖用户当前编辑内容的场景。

## V1-7.3：生成配置模板化（已完成）

日期：2026-05-21

### 目标
减少每次手动调参数的重复劳动，让常用生成策略可一键套用。

### 本阶段完成
- 写作页“生成指令”面板新增“生成模板”下拉。
- 内置模板：
  - 修仙主线推进
  - 战斗章节
  - 日常过渡
  - 情绪与关系
- 模板可一键套用以下参数：
  - 目标字数下限/上限
  - 最近章节参考数
  - 风格约束开关
  - 指令骨架（当当前指令为空时自动填入）
- 按小说维度本地记住上次选择的模板。

### 运行与验证
- `cd frontend && npm run test -- --run`
- `cd frontend && npm run build`

### 复习要点
- 先模板化“参数组合”，后续可再模板化“人物/设定选择策略”。

## V1-8.1：用户级 AI 设置（已完成）

日期：2026-05-21

### 目标
不再要求每次通过终端 `export` 才能生成，让用户可在页面直接配置 OpenAI 参数。

### 本阶段完成
- 后端新增用户 AI 设置表：`user_ai_settings`。
- 新增接口：
  - `GET /users/me/ai-settings`
  - `PUT /users/me/ai-settings`
- API Key 仅加密存储，不回显明文；前端仅显示掩码。
- 章节生成链路改为：
  - 优先读取用户级配置（Key/Base URL/Model）
  - 未配置时回退服务端环境变量。
- 前端用户菜单新增“AI 设置”弹窗，可直接保存配置。

### 运行与验证
- `cd backend && go test ./...`
- `cd frontend && npm run build`

### 复习要点
- 把“环境配置能力”产品化到页面后，体验门槛明显下降。
- 密钥处理必须遵循“后端加密存储 + 前端掩码回显”最小安全原则。

## V1-8.2：写作页直达 AI 设置与错误引导（已完成）

日期：2026-05-21

### 目标
减少“生成失败后再回主页配置”的跳转成本，让用户在写作页内完成配置修复。

### 本阶段完成
- 写作页顶部/底部操作区新增 `AI 设置` 按钮，直接打开配置弹窗。
- 复用用户级 AI 设置接口，写作页可直接保存 `API Key / Base URL / Model`。
- 错误提示优化：
  - `openai api key is not configured` 映射为中文引导文案。
  - 生成失败提示补充“写作页右上角 AI 设置”入口指引。
- 修复 migration 外键类型不一致：
  - `user_ai_settings.user_id` 改为 `BIGINT UNSIGNED`，与 `users.id` 对齐。

### 运行与验证
- `cd frontend && npm run build`
- `docker compose exec -T mysql mysql -unovel -pnovel novel_agent < backend/migrations/202605210001_create_user_ai_settings.up.sql`
- `docker compose exec -T mysql mysql -unovel -pnovel novel_agent -e "SHOW TABLES LIKE 'user_ai_settings'; SHOW COLUMNS FROM user_ai_settings;"`

### 复习要点
- “可操作的错误提示 + 就地修复入口”比单纯报错更重要。
- 新增外键时要先确认主外键字段类型完全一致（含 unsigned）。
